# Base URL of the backend API. Defaults to same-origin "/api", which works
# both with the Vite dev proxy and nginx in production.
VITE_API_URL=/api

# Bot username (without @) used by the Telegram Login Widget on the sign-in screen.
# Leave empty to hide the "Войти через Telegram" button.
VITE_TELEGRAM_BOT=
